name: Backend Tests

on:
  push:
    branches: ["main"]
    paths:
      - "backend_fixed/**"
      - ".github/workflows/tests.yml"
  pull_request:
    branches: ["main"]
    paths:
      - "backend_fixed/**"
      - ".github/workflows/tests.yml"

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: |
            backend_fixed/requirements.txt
            backend_fixed/requirements-dev.txt

      - name: Install dependencies
        working-directory: backend_fixed
        run: pip install -r requirements.txt -r requirements-dev.txt

      - name: Run tests
        working-directory: backend_fixed
        env:
          OPENAI_API_KEY: sk-test-not-real
        run: pytest tests/ -v --tb=short
