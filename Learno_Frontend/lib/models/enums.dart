
enum Grade {
  kindergarten,
  first,
  second,
  third,
  fourth,
}

enum Subject {
  english,
  arabic,
  science,
  math,
  stories,
}

enum ResponseType {
  intro,
  question,
  praise,
  encouragement,
  hint,
  redirect,
  welcome,
  resume,
  explanation,
  celebration,
}

enum SessionStatus {
  idle,
  active,
  complete,
  error,
}

enum ChatMode {
  voice,
  text,
}

enum LearningLevel {
  struggling,
  developing,
  proficient,
  advanced,
}

enum TeachingStyle {
  extraSupport,
  standard,
  accelerated,
  challenge,
}
